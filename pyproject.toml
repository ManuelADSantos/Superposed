[project]
name = "superposed"
version = "1.0"
description = "A quantum computing puzzle game inspired by factory builders"
requires-python = ">=3.9"
dependencies = [
    "pygame>=2.1",
]

[project.scripts]
superposed = "src.main:main"
